{
  "name": "dev-task",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ nanspro/dev-task",
    "create-local": "graph create --node http://localhost:8020/ nanspro/dev-task",
    "remove-local": "graph remove --node http://localhost:8020/ nanspro/dev-task",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nanspro/dev-task"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
